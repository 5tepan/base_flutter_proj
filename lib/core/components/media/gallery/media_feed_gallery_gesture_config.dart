import 'package:extended_image/extended_image.dart';

GestureConfig get mediaFeedGalleryGestureConfig => GestureConfig(
  inPageView: true,
  minScale: 1,
  maxScale: 4,
  animationMinScale: 0.8,
  animationMaxScale: 4,
  initialScale: 1,
);
