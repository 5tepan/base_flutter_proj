# Локальные секреты (не коммитить). Скопируйте: cp .env.example .env
# Используется scripts/setup_secrets.sh и CI-скриптами.

# Apple Developer Team ID (Signing)
IOS_DEVELOPMENT_TEAM=

# Firebase App Distribution (scripts/ci_*_dev_distribution.sh)
FIREBASE_ANDROID_APP_ID=
FIREBASE_IOS_APP_ID=
